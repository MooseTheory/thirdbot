version: '3.6'

services:
  db:
    image: mariadb
    restart: always
    environment:
      MYSQL_DATABASE: thirdbot
      MYSQL_USER: thirdbot
      MYSQL_PASSWORD: E3K7dFxcz9LbCeRj
      MYSQL_RANDOM_ROOT_PASSWORD: 'yes'

  thirdbot:
    image: moosetheory/thirdbot:dev
    restart: always
    volumes:
      - "./config.toml.dev:/config.toml"